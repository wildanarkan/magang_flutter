import 'package:flutter/material.dart';

class BuildLeaveHistory extends StatelessWidget {
  const BuildLeaveHistory({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}